# Monorepo: run lint-staged inside each package so ESLint/Prettier resolve that
# package's own config and binaries. Each run only touches its own staged files.
(cd backend && npx lint-staged) && (cd frontend && npx lint-staged)
